{
  "name": "ptrck-io",
  "description": "ptrck.io website",
  "license": "MIT",
  "scripts": {
    "develop": "gatsby develop",
    "build": "gatsby build",
    "deploy": "gatsby build && gh-pages -d public --branch master"
  },
  "dependencies": {
    "gatsby": "^1.9.250",
    "gatsby-link": "^1.6.40",
    "gatsby-plugin-sass": "1.0.26"
  },
  "devDependencies": {
    "gh-pages": "1.2.0"
  }
}
